{# -*- coding: utf-8 -*- #}
% extends "forms/user_tab.html"

% load exfiltry
% load exsyntax

%% all
    %with scroll_table=True no_id=True title="Macież SOD:"
        {{ block.super }}

%% list_row_header
    th...Funkcja praw
    % for action in actions:
        th...{{action}}

%% list_row_header_end

%% list_table
    % for row in object_list:
        tr
            % for pos in row:
                td... {{ pos|safe }}
