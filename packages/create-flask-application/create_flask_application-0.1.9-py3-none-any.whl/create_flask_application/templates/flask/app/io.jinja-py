from .utils import CustomSocketIo

socketio = CustomSocketIo(engineio_logger=True, logger=True,
                          cors_allowed_origins="*",
                          cors_credentials=True)

