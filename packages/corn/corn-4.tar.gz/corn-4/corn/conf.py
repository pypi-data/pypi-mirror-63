CLI_CONFIG = {
    "items": {
        "positional": True,
        "default": [],
        "nargs": "*",
        "help": "Print the named corn",
        "type": str,
    },
    "out": {
        "default": "nested",
        "type": str,
        "help": "The outputter to display the corn",
    },
}
CONFIG = {}
GLOBAL = {}
SUBS = {}
DYNE = {
    "corn": ["corn"],
}
