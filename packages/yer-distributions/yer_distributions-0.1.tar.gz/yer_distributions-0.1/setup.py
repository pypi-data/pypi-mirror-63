from setuptools import setup

setup(name='yer_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['yer_distributions'],
      zip_safe=False,
      install_requires=['matplotlib'])
